services:
  - type: web
    name: zumba-backend
    runtime: docker
    rootDir: backend
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: SHOPIFY_WEBHOOK_SECRET
        sync: false
